apiVersion: v1
kind: Service
metadata:
  name: trade-mcp
  namespace: trade-mcp
  labels:
    app: trade-mcp
spec:
  selector:
    app: trade-mcp
  ports:
    - name: http
      port: 8002
      targetPort: http
      protocol: TCP
  type: ClusterIP
